name: Android CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle

    - name: Generate gradlew and initialize Gradle if missing
      run: |
        if [ ! -f gradlew ]; then
          gradle init
          gradle wrapper
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add gradlew gradlew.bat gradle/wrapper build.gradle settings.gradle
          git commit -m "Initialize Gradle project and add Gradle wrapper"
          git push
        fi

    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew

    - name: Build with Gradle
      run: ./gradlew build
